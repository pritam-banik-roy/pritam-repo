CREATE DATABASE flight_reservation;

USE flight_reservation;

CREATE TABLE flights (
    id INT PRIMARY KEY AUTO_INCREMENT,
    flight_number VARCHAR(20),
    source VARCHAR(50),
    destination VARCHAR(50),
    price DOUBLE
);



INSERT INTO flights (flight_number, source, destination, price)
VALUES
('AI101', 'Kolkata', 'Delhi', 5000),
('AI102', 'Kolkata', 'Mumbai', 6500),
('AI103', 'Delhi', 'Mumbai', 4500),
('AI104', 'Kolkata', 'Delhi', 5500);





application.properties

db.driver=com.mysql.cj.jdbc.Driver
db.url=jdbc:mysql://localhost:3306/flight_reservation
db.username=root
db.password=yourpassword
